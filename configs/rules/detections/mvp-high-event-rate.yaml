id: mvp_high_event_rate
name: MVP High Event Rate
description: Fallback rule for unexpected ingest surges over the last minute.
severity: low
enabled: true
threshold_op: gte
threshold_value: 120
query: |
  SELECT count()
  FROM security.events
  WHERE ts > now() - INTERVAL 1 MINUTE
